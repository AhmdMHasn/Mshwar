<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_trips_all"
            android:icon="@drawable/nav_all"
            android:title="@string/str_trips_all" />
        <item
            android:id="@+id/nav_trips_upcoming"
            android:icon="@drawable/nav_upcoming"
            android:title="@string/str_trips_upcoming" />
        <item
            android:id="@+id/nav_trips_done"
            android:icon="@drawable/nav_done"
            android:title="@string/str_trips_done" />
        <item
            android:id="@+id/nav_trips_cancelled"
            android:icon="@drawable/nav_cancelled"
            android:title="@string/str_trips_cancelled" />
    </group>

    <item android:title="@string/str_navigation_user">
        <menu>
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/settings"
                android:title="@string/str_user_settings" />
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/logout"
                android:title="@string/str_user_logout" />
        </menu>
    </item>

</menu>
